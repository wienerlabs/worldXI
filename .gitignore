# Dependencies
node_modules
.yarn

# Build output
target
dist
**/dist
*.tsbuildinfo
**/*.rs.bk

# Anchor / local validator
.anchor
test-ledger

# Secrets (NEVER commit)
.env
**/.env
*.env.local
**/*keypair*.json
**/id.json
wallets

# Editor / OS
.DS_Store
.claude
.idea
.vscode

# Scratch / temp
1.jpeg
scratchpad
worldcup.png

# NOTE: app/public/players.json and countries.json are intentionally COMMITTED.
# The copy-data script refreshes them from data/ during local dev, but committing them
# means a cloud build (Vercel) never depends on reaching ../data outside its root dir.
