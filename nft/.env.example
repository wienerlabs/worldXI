# Solana RPC (devnet)
RPC_URL=https://api.devnet.solana.com
# cNFT mint yetkilisi / ağaç sahibi keypair
NFT_KEYPAIR=/Users/makinci/.config/solana/id.json
# create-tree çıktısındaki merkle tree adresi
MERKLE_TREE=
# Metadata URI tabanı (deploy edilmiş oracle API). Boşsa data URI kullanılır.
NFT_METADATA_BASE=
